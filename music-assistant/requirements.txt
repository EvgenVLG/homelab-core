# music-assistant v0.1
#
# Core: standard library only. No heavy dependencies required.
#
# ── Optional ─────────────────────────────────────────────────────────────────
#
# mutagen: read ID3/FLAC/Ogg tags for better artist/title extraction.
# The library scanner works without it (path-based inference), but
# tag data meaningfully improves match quality on messy libraries.
#
# Uncomment and rebuild to enable:
# mutagen==1.47.*
